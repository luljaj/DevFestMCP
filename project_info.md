# Complete System Architecture: Agent Coordination Platform
## Visual Architecture Diagrams (No Code)

---

## Diagram 1: High-Level System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                        BROWSER / CLIENT LAYER                               │
│                                                                             │
│  ┌────────────────────────────────────┐  ┌─────────────────────────────┐  │
│  │     Graph Visualization UI         │  │   Agent Activity Feed       │  │
│  │                                    │  │   (Agent Slack)             │  │
│  │  📁 src/auth/  🔴 Luka WRITING    │  │                             │  │
│  │  ┌──────┐   ┌──────┐   ┌──────┐   │  │  🔧 Luka: Auth refactor     │  │
│  │  │ File │──▶│ Func │──▶│ Func │   │  │     complete, running tests │  │
│  │  │  A   │   │  1   │   │  2   │   │  │     Layer: backend/auth     │  │
│  │  └──────┘   └─🔴───┘   └──────┘   │  │                             │  │
│  │   auth.ts   validate   reset      │  │  🔍 Jane: Investigating DB  │  │
│  │             Token      Password    │  │     timeout issue           │  │
│  │             (Luka)     (OPEN)      │  │     Layer: backend/db       │  │
│  │                                    │  │                             │  │
│  │  📁 src/db/  🔵 Jane READING      │  │  [Real-time updates]        │  │
│  └────────────────────────────────────┘  └─────────────────────────────┘  │
│                       ▲                              ▲                      │
│                       │                              │                      │
│                       └──────────────┬───────────────┘                      │
│                                      │                                      │
└──────────────────────────────────────┼──────────────────────────────────────┘
                                       │
                                       │ WebSocket (wss://)
                                       │ Real-time bidirectional
                                       │
┌──────────────────────────────────────┼──────────────────────────────────────┐
│                                      │                                      │
│                      STREAMING LAYER (FastAPI + WebSocket)                 │
│                                      │                                      │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │              WebSocket Connection Manager                            │  │
│  │                                                                      │  │
│  │  • Maintains active connections: Set[WebSocket]                     │  │
│  │  • Broadcasts events to all clients                                 │  │
│  │  • Handles client connect/disconnect                                │  │
│  │  • Streams real-time lock table updates                             │  │
│  │                                                                      │  │
│  │  Event Types Broadcast:                                             │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │  status_update    → User posted new status (OPEN/READ/WRITE) │  │  │
│  │  │  activity_posted  → User posted to activity feed             │  │  │
│  │  │  lock_expired     → Lock timed out (no heartbeat)            │  │  │
│  │  │  conflict_warning → Multiple users on same symbol            │  │  │
│  │  │  dependency_warning → Dependency being written               │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                      │                                      │
└──────────────────────────────────────┼──────────────────────────────────────┘
                                       │
                                       │ Internal Event Bus
                                       │
┌──────────────────────────────────────┼──────────────────────────────────────┐
│                                      ▼                                      │
│                   COORDINATION LOGIC LAYER (MCP Server)                    │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                    🔐 Authentication Pipeline                         │ │
│  │                                                                       │ │
│  │  Every MCP Tool Call Flow:                                           │ │
│  │                                                                       │ │
│  │  1. Tool invoked by agent                                            │ │
│  │      ↓                                                                │ │
│  │  2. get_context() extracts decrypted credentials                     │ │
│  │      ↓                                                                │ │
│  │  3. context.request_context.credentials["COORD_API_KEY"]             │ │
│  │      ↓                                                                │ │
│  │  4. Hash API key → Lookup in user database                           │ │
│  │      ↓                                                                │ │
│  │  5. Return UserInfo { user_id, email, name }                         │ │
│  │      ↓                                                                │ │
│  │  6. Tool executes with authenticated user context                    │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                      MCP Tools (Pydantic Models)                      │ │
│  │                                                                       │ │
│  │  @tool(description="...")                                            │ │
│  │  async def check_interference(                                       │ │
│  │      request: CheckInterferenceRequest                               │ │
│  │  ) -> CheckInterferenceResponse:                                     │ │
│  │      ctx = get_context()                    ← Dedalus provides this │ │
│  │      user = authenticate_from_context(ctx)  ← Extract who called    │ │
│  │      await ctx.info("Checking conflicts")   ← Log progress          │ │
│  │      conflicts = detect_conflicts(...)      ← Business logic        │ │
│  │      return CheckInterferenceResponse(...)  ← Type-safe response    │ │
│  │                                                                       │ │
│  │  Available Tools:                                                    │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐│ │
│  │  │ • check_interference(symbols: List[str])                        ││ │
│  │  │   → Returns conflicts + dependency warnings                     ││ │
│  │  │                                                                  ││ │
│  │  │ • post_status(symbol, status, message)                          ││ │
│  │  │   → Status: "OPEN" | "READING" | "WRITING"                      ││ │
│  │  │   → Updates lock table + broadcasts                             ││ │
│  │  │   → If OPEN: queries git for last commit timestamp              ││ │
│  │  │                                                                  ││ │
│  │  │ • heartbeat(symbols: List[str])                                 ││ │
│  │  │   → Keeps locks alive                                           ││ │
│  │  │                                                                  ││ │
│  │  │ • post_activity(message, layer, task_type)                      ││ │
│  │  │   → Post to agent activity feed (like Slack)                    ││ │
│  │  │                                                                  ││ │
│  │  │ • get_activity_feed(since_timestamp)                            ││ │
│  │  │   → Pull recent activity updates                                ││ │
│  │  │                                                                  ││ │
│  │  │ • get_folder_activity()                                         ││ │
│  │  │   → Returns aggregated folder-level status                      ││ │
│  │  └─────────────────────────────────────────────────────────────────┘│ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                     State Management Layer                            │ │
│  │                                                                       │ │
│  │  In-Memory Data Structures:                                          │ │
│  │                                                                       │ │
│  │  ┌─────────────────────────────────────────────────────────────┐    │ │
│  │  │ lock_table: Dict[str, LockEntry]                            │    │ │
│  │  │                                                              │    │ │
│  │  │ Status: "OPEN" | "READING" | "WRITING"                      │    │ │
│  │  │                                                              │    │ │
│  │  │ {                                                            │    │ │
│  │  │   "auth.ts::validateToken": {                               │    │ │
│  │  │     symbol: "auth.ts::validateToken",                       │    │ │
│  │  │     user_id: "luka",                                        │    │ │
│  │  │     user_email: "luka@example.com",                         │    │ │
│  │  │     user_name: "Luka",                                      │    │ │
│  │  │     status: "WRITING",                                      │    │ │
│  │  │     message: "Refactoring to JWT",                          │    │ │
│  │  │     timestamp: 1234567890.123,                              │    │ │
│  │  │     last_heartbeat: 1234567920.456                          │    │ │
│  │  │   },                                                         │    │ │
│  │  │   "auth.ts::login": {                                       │    │ │
│  │  │     symbol: "auth.ts::login",                               │    │ │
│  │  │     status: "OPEN",  ← Not being worked on                 │    │ │
│  │  │     user_id: null,                                          │    │ │
│  │  │     last_commit_sha: "abc123def",                           │    │ │
│  │  │     last_commit_time: 1234567800.0,                         │    │ │
│  │  │     last_commit_author: "jane",                             │    │ │
│  │  │     last_commit_message: "feat: improve login flow"         │    │ │
│  │  │   }                                                          │    │ │
│  │  │ }                                                            │    │ │
│  │  └─────────────────────────────────────────────────────────────┘    │ │
│  │                                                                       │ │
│  │  ┌─────────────────────────────────────────────────────────────┐    │ │
│  │  │ user_database: Dict[str, UserInfo]                          │    │ │
│  │  │                                                              │    │ │
│  │  │ {                                                            │    │ │
│  │  │   sha256("coord_sk_luka_abc123"): {                         │    │ │
│  │  │     user_id: "luka",                                        │    │ │
│  │  │     email: "luka@example.com",                              │    │ │
│  │  │     name: "Luka"                                            │    │ │
│  │  │   },                                                         │    │ │
│  │  │   sha256("coord_sk_jane_xyz789"): {                         │    │ │
│  │  │     user_id: "jane",                                        │    │ │
│  │  │     email: "jane@example.com",                              │    │ │
│  │  │     name: "Jane"                                            │    │ │
│  │  │   }                                                          │    │ │
│  │  │ }                                                            │    │ │
│  │  └─────────────────────────────────────────────────────────────┘    │ │
│  │                                                                       │ │
│  │  ┌─────────────────────────────────────────────────────────────┐    │ │
│  │  │ activity_feed: List[ActivityEntry]                          │    │ │
│  │  │                                                              │    │ │
│  │  │ Agent Slack - High-level activity updates                   │    │ │
│  │  │                                                              │    │ │
│  │  │ [                                                            │    │ │
│  │  │   {                                                          │    │ │
│  │  │     id: "activity_001",                                     │    │ │
│  │  │     user_id: "luka",                                        │    │ │
│  │  │     user_name: "Luka",                                      │    │ │
│  │  │     message: "Moving to database layer",                    │    │ │
│  │  │     timestamp: 1234567890.0,                                │    │ │
│  │  │     context: {                                              │    │ │
│  │  │       layer: "backend/db",                                  │    │ │
│  │  │       task_type: "refactor"                                 │    │ │
│  │  │     }                                                        │    │ │
│  │  │   }                                                          │    │ │
│  │  │ ]                                                            │    │ │
│  │  └─────────────────────────────────────────────────────────────┘    │ │
│  │                                                                       │ │
│  │  ┌─────────────────────────────────────────────────────────────┐    │ │
│  │  │ dependency_graph: Dict[str, List[str]]                      │    │ │
│  │  │                                                              │    │ │
│  │  │ {                                                            │    │ │
│  │  │   "auth.ts::login": [                                       │    │ │
│  │  │     "auth.ts::validateToken",                               │    │ │
│  │  │     "db.ts::query"                                          │    │ │
│  │  │   ],                                                         │    │ │
│  │  │   "auth.ts::validateToken": [                               │    │ │
│  │  │     "jwt.ts::verify"                                        │    │ │
│  │  │   ]                                                          │    │ │
│  │  │ }                                                            │    │ │
│  │  └─────────────────────────────────────────────────────────────┘    │ │
│  │                                                                       │ │
│  │  ┌─────────────────────────────────────────────────────────────┐    │ │
│  │  │ folder_activity: Dict[str, FolderStatus] (computed)         │    │ │
│  │  │                                                              │    │ │
│  │  │ Aggregated view of which folders agents are working in      │    │ │
│  │  │                                                              │    │ │
│  │  │ {                                                            │    │ │
│  │  │   "src/auth/": {                                            │    │ │
│  │  │     users: ["Luka"],                                        │    │ │
│  │  │     symbols: ["validateToken", "login"],                    │    │ │
│  │  │     statuses: ["WRITING"]                                   │    │ │
│  │  │   },                                                         │    │ │
│  │  │   "src/db/": {                                              │    │ │
│  │  │     users: ["Jane"],                                        │    │ │
│  │  │     symbols: ["query"],                                     │    │ │
│  │  │     statuses: ["READING"]                                   │    │ │
│  │  │   }                                                          │    │ │
│  │  │ }                                                            │    │ │
│  │  └─────────────────────────────────────────────────────────────┘    │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                   Background Tasks (Async)                            │ │
│  │                                                                       │ │
│  │  cleanup_stale_locks():                                              │ │
│  │  ├─ Runs every 10 seconds                                            │ │
│  │  ├─ Checks last_heartbeat for each lock                              │ │
│  │  ├─ If > 60 seconds since heartbeat → Expire lock                    │ │
│  │  ├─ Broadcast "lock_expired" event                                   │ │
│  │  └─ Remove from lock_table                                           │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       │ MCP Protocol
                                       │ (JSON-RPC over HTTPS/SSE)
                                       │
┌──────────────────────────────────────┼──────────────────────────────────────┐
│                                      ▼                                      │
│                    DEDALUS INFRASTRUCTURE LAYER                            │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │              Credential Encryption/Decryption Pipeline                │ │
│  │                                                                       │ │
│  │  Client sends request:                                               │ │
│  │  {                                                                    │ │
│  │    tool: "check_interference",                                       │ │
│  │    params: { symbols: [...] },                                       │ │
│  │    credentials: {                                                    │ │
│  │      "COORD_API_KEY": "aG4kL9mPxY2zQ=="  ← Encrypted blob           │ │
│  │    }                                                                 │ │
│  │  }                                                                    │ │
│  │                                                                       │ │
│  │  Dedalus Pipeline:                                                   │ │
│  │  1. Receive encrypted credential blob                                │ │
│  │  2. Route request to target MCP server                               │ │
│  │  3. Just-In-Time decryption in secure enclave                        │ │
│  │  4. Pass decrypted credential to MCP tool in context                 │ │
│  │  5. Tool executes with plaintext credential                          │ │
│  │  6. Credential discarded after tool completes                        │ │
│  │                                                                       │ │
│  │  Security Properties:                                                │ │
│  │  ✓ Credentials never stored in plaintext                            │ │
│  │  ✓ Decryption only at runtime (JIT)                                 │ │
│  │  ✓ Isolated secure enclave environment                              │ │
│  │  ✓ Multi-tenant by default                                          │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                    DedalusRunner Orchestration                        │ │
│  │                                                                       │ │
│  │  Handles:                                                            │ │
│  │  • Model routing (GPT-5, Claude Opus, etc.)                          │ │
│  │  • MCP server connection management                                  │ │
│  │  • Tool call formatting & execution                                  │ │
│  │  • Streaming response coordination                                   │ │
│  │  • Error handling & retries                                          │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       │ Agent API Calls
                                       │
┌──────────────────────────────────────┼──────────────────────────────────────┐
│                                      ▼                                      │
│                         AGENT EXECUTION LAYER                              │
│                                                                             │
│  ┌─────────────────────────────────────┐  ┌─────────────────────────────┐ │
│  │    Cursor Instance 1 (Luka)         │  │  Cursor Instance 2 (Jane)   │ │
│  │                                     │  │                             │ │
│  │  Environment:                       │  │  Environment:               │ │
│  │  .env:                              │  │  .env:                      │ │
│  │    COORD_API_KEY=                   │  │    COORD_API_KEY=           │ │
│  │      coord_sk_luka_abc123           │  │      coord_sk_jane_xyz789   │ │
│  │    DEDALUS_API_KEY=<shared>         │  │    DEDALUS_API_KEY=<shared> │ │
│  │                                     │  │                             │ │
│  │  ┌────────────────────────────────┐ │  │ ┌─────────────────────────┐ │ │
│  │  │  Agent Setup Code              │ │  │ │  Agent Setup Code       │ │ │
│  │  │                                │ │  │ │                         │ │ │
│  │  │  from dedalus_mcp.auth import │ │  │ │  (Same setup,           │ │ │
│  │  │    Connection, SecretValues   │ │  │ │   different .env)       │ │ │
│  │  │                                │ │  │ │                         │ │ │
│  │  │  coord = Connection(          │ │  │ │                         │ │ │
│  │  │    name="coord-server",       │ │  │ │                         │ │ │
│  │  │    secrets=SecretKeys(        │ │  │ │                         │ │ │
│  │  │      key="COORD_API_KEY"      │ │  │ │                         │ │ │
│  │  │    )                           │ │  │ │                         │ │ │
│  │  │  )                             │ │  │ │                         │ │ │
│  │  │                                │ │  │ │                         │ │ │
│  │  │  coord_secrets = SecretValues(│ │  │ │                         │ │ │
│  │  │    coord,                     │ │  │ │                         │ │ │
│  │  │    key=os.getenv(             │ │  │ │                         │ │ │
│  │  │      "COORD_API_KEY"          │ │  │ │                         │ │ │
│  │  │    )                           │ │  │ │                         │ │ │
│  │  │  )                             │ │  │ │                         │ │ │
│  │  │                                │ │  │ │                         │ │ │
│  │  │  ↓ Encrypts locally:          │ │  │ │  ↓ Encrypts locally:   │ │ │
│  │  │  coord_sk_luka_abc123         │ │  │ │  coord_sk_jane_xyz789  │ │ │
│  │  │  → aG4kL9m... (blob)           │ │  │ │  → xB2mK8p... (blob)   │ │ │
│  │  └────────────────────────────────┘ │  │ └─────────────────────────┘ │ │
│  │                                     │  │                             │ │
│  │  ┌────────────────────────────────┐ │  │ ┌─────────────────────────┐ │ │
│  │  │  Claude Agent                  │ │  │ │  Claude Agent           │ │ │
│  │  │                                │ │  │ │                         │ │ │
│  │  │  System Prompt:                │ │  │ │  System Prompt:         │ │ │
│  │  │  "Before modifying code,       │ │  │ │  (Same prompt)          │ │ │
│  │  │   MUST call                    │ │  │ │                         │ │ │
│  │  │   check_interference"          │ │  │ │                         │ │ │
│  │  │                                │ │  │ │                         │ │ │
│  │  │  User: "Refactor auth"         │ │  │ │  User: "Add pwd reset"  │ │ │
│  │  │    ↓                           │ │  │ │    ↓                    │ │ │
│  │  │  Agent reasoning:              │ │  │ │  Agent reasoning:       │ │ │
│  │  │  1. Need to modify:            │ │  │ │  1. Need to modify:     │ │ │
│  │  │     - auth.ts::validateToken   │ │  │ │     - auth.ts::validate │ │ │
│  │  │     - auth.ts::login           │ │  │ │     - auth.ts::reset    │ │ │
│  │  │  2. Must check first!          │ │  │ │  2. Must check first!   │ │ │
│  │  │    ↓                           │ │  │ │    ↓                    │ │ │
│  │  │  Calls MCP tool:               │ │  │ │  Calls MCP tool:        │ │ │
│  │  │  check_interference([...])     │ │  │ │  check_interference([]) │ │ │
│  │  └────────────────────────────────┘ │  │ └─────────────────────────┘ │ │
│  │                                     │  │                             │ │
│  │  Current Task:                      │  │  Current Task:              │ │
│  │  Refactoring auth to JWT            │  │  Adding password reset      │ │
│  └─────────────────────────────────────┘  └─────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Diagram 2: Authentication Flow (Dedalus Auth Deep Dive)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    CREDENTIAL LIFECYCLE: START TO FINISH                    │
└─────────────────────────────────────────────────────────────────────────────┘

PHASE 1: Client-Side Setup (Luka's Machine)
═══════════════════════════════════════════

┌──────────────────────────────────────┐
│ Step 1: Load from .env file          │
│                                      │
│ .env file on disk:                   │
│ ┌──────────────────────────────────┐ │
│ │ COORD_API_KEY=coord_sk_luka_abc12│ │
│ │ DEDALUS_API_KEY=dls_xxx...       │ │
│ └──────────────────────────────────┘ │
│                                      │
│ Python reads:                        │
│ api_key = os.getenv("COORD_API_KEY") │
│ # api_key = "coord_sk_luka_abc123"  │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ Step 2: Define Connection            │
│                                      │
│ coord_connection = Connection(       │
│     name="coordination-server",     │
│     secrets=SecretKeys(             │
│         key="COORD_API_KEY"         │
│     )                               │
│ )                                    │
│                                      │
│ This tells Dedalus:                 │
│ "When connecting to coord-server,   │
│  look for credential named           │
│  COORD_API_KEY"                     │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ Step 3: Bind Credential              │
│                                      │
│ coord_secrets = SecretValues(        │
│     coord_connection,               │
│     key="coord_sk_luka_abc123"      │
│ )                                    │
│                                      │
│ ⚡ CLIENT-SIDE ENCRYPTION HAPPENS:   │
│                                      │
│ Plaintext:                          │
│   coord_sk_luka_abc123              │
│      ↓                               │
│ AES-256 Encryption (local only)     │
│      ↓                               │
│ Encrypted blob:                     │
│   aG4kL9mPxY2zQ8vT3nM1kL==          │
│                                      │
│ ✓ Never leaves machine in plaintext │
│ ✓ Encryption key never sent         │
│ ✓ Only Dedalus can decrypt          │
└──────────────────┬───────────────────┘
                   │
                   ▼

PHASE 2: Network Transit
═══════════════════════════

┌──────────────────────────────────────┐
│ Step 4: Agent Calls MCP Tool         │
│                                      │
│ runner.run(                          │
│     input="Check conflicts",        │
│     mcp_servers=[                   │
│         "user/coordination-server"  │
│     ],                              │
│     credentials=[coord_secrets]     │
│ )                                    │
│                                      │
│ Request sent over HTTPS:            │
│ {                                   │
│   "tool": "check_interference",     │
│   "params": {                       │
│     "symbols": ["auth.ts::login"]   │
│   },                                │
│   "credentials": {                  │
│     "COORD_API_KEY":                │
│       "aG4kL9mPxY2zQ8vT3nM1kL=="    │
│   }                    ↑             │
│ }                      │             │
│                        │             │
│              Still encrypted!        │
└──────────────────┬───────────────────┘
                   │
                   │ HTTPS (TLS 1.3)
                   │ Double encryption:
                   │ 1. Dedalus encryption
                   │ 2. HTTPS tunnel
                   │
                   ▼

PHASE 3: Dedalus Infrastructure
═══════════════════════════════════

┌──────────────────────────────────────┐
│ Step 5: Dedalus Receives Request     │
│                                      │
│ Router identifies:                   │
│ • Target: "user/coordination-server" │
│ • Tool: "check_interference"         │
│ • Has encrypted credentials          │
│                                      │
│ Routing decision:                    │
│ → Forward to user's MCP server       │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ Step 6: Just-In-Time Decryption      │
│                                      │
│ 🔒 Secure Enclave Environment        │
│                                      │
│ ONLY when tool is about to execute:  │
│                                      │
│ 1. Load encrypted blob:              │
│    aG4kL9mPxY2zQ8vT3nM1kL==          │
│                                      │
│ 2. Decrypt using Dedalus key:        │
│    (Happens in isolated memory)      │
│      ↓                               │
│    coord_sk_luka_abc123              │
│                                      │
│ 3. Place in context object:          │
│    context = {                       │
│        request_context: {            │
│            credentials: {            │
│                "COORD_API_KEY":      │
│                  "coord_sk_luka_abc" │
│            }                         │
│        }                             │
│    }                                 │
│                                      │
│ 4. Pass to MCP tool function         │
│                                      │
│ ⚠️ CRITICAL SECURITY PROPERTIES:     │
│ • Plaintext only exists in memory   │
│ • Only during tool execution        │
│ • Never written to disk             │
│ • Never logged                      │
│ • Discarded immediately after       │
└──────────────────┬───────────────────┘
                   │
                   ▼

PHASE 4: MCP Server Execution
═══════════════════════════════

┌──────────────────────────────────────┐
│ Step 7: Tool Receives Context        │
│                                      │
│ @tool(description="Check conflicts") │
│ async def check_interference(        │
│     request: CheckInterferenceReq,   │
│ ):                                   │
│     ctx = get_context()              │
│                                      │
│     # Extract plaintext credential   │
│     api_key = ctx.request_context    │
│         .credentials["COORD_API_KEY"]│
│                                      │
│     # api_key is now:                │
│     # "coord_sk_luka_abc123"         │
│     #         ↑                      │
│     #    Plaintext!                  │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ Step 8: Authenticate User            │
│                                      │
│ def authenticate(api_key: str):      │
│     # Hash the API key               │
│     key_hash = sha256(api_key)       │
│     # key_hash = "7a3f2b..."         │
│                                      │
│     # Lookup in user database        │
│     user = USERS[key_hash]           │
│                                      │
│     # Returns:                       │
│     # {                              │
│     #   user_id: "luka",             │
│     #   email: "luka@example.com",   │
│     #   name: "Luka"                 │
│     # }                              │
│                                      │
│ ✓ Now we know WHO made this request │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ Step 9: Execute Business Logic       │
│                                      │
│ conflicts = []                       │
│                                      │
│ for symbol in request.symbols:       │
│     if symbol in lock_table:         │
│         lock = lock_table[symbol]    │
│                                      │
│         # Don't conflict with self   │
│         if lock.user_id == user_id:  │
│             continue                 │
│                                      │
│         conflicts.append({           │
│             "held_by_email":         │
│                 lock.user_email,     │
│             "status": lock.status    │
│         })                           │
│                                      │
│ return CheckInterferenceResponse(    │
│     your_email=user.email,           │
│     conflicts=conflicts              │
│ )                                    │
└──────────────────┬───────────────────┘
                   │
                   ▼

PHASE 5: Cleanup
═══════════════════

┌──────────────────────────────────────┐
│ Step 10: Credential Disposal         │
│                                      │
│ After tool completes:                │
│                                      │
│ 1. Return result to Dedalus          │
│ 2. Context object destroyed          │
│ 3. Plaintext credential removed      │
│    from memory                       │
│ 4. No trace of plaintext remains     │
│                                      │
│ Next tool call:                      │
│ • Repeat entire process              │
│ • Fresh decryption                   │
│ • No persistent plaintext            │
└──────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

SECURITY PROPERTIES SUMMARY:

✅ Client-Side Encryption
   → API key encrypted before leaving user's machine
   
✅ Zero-Knowledge Architecture
   → Dedalus never sees plaintext during storage/transit
   
✅ Just-In-Time Decryption
   → Plaintext only exists during tool execution
   
✅ Secure Enclave Isolation
   → Decryption happens in isolated environment
   
✅ No Persistence
   → Plaintext never written to disk/logs
   
✅ Multi-Tenant by Default
   → Each user has unique encrypted blob
   → Server can't confuse users
   
✅ Credential Rotation Ready
   → User can change API key anytime
   → Just update .env file
   → No server-side changes needed

═══════════════════════════════════════════════════════════════════════════════
```

---

## Diagram 3: Real-Time Conflict Detection Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                 CONFLICT DETECTION: TWO AGENTS, ONE FUNCTION                │
└─────────────────────────────────────────────────────────────────────────────┘

TIME: T=0
══════════

┌─────────────────────────────┐       ┌─────────────────────────────┐
│  Agent A (Luka)             │       │  Agent B (Jane)             │
│                             │       │                             │
│  User: "Refactor auth"      │       │  User: "Add password reset" │
│                             │       │                             │
│  Agent thinks:              │       │  Agent thinks:              │
│  "I need to modify:         │       │  "I need to modify:         │
│   • auth.ts::validateToken  │       │   • auth.ts::validateToken  │
│   • auth.ts::login"         │       │   • auth.ts::resetPassword" │
│                             │       │                             │
│  ⚠️ Both want validateToken │       │  ⚠️ Both want validateToken │
└─────────────┬───────────────┘       └─────────────┬───────────────┘
              │                                     │
              │ check_interference()                │ check_interference()
              │ (with Luka's credentials)           │ (with Jane's credentials)
              │                                     │
              ▼                                     ▼

TIME: T=1 (Luka checks first)
═══════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│              MCP Server: Lock Table                         │
│                                                             │
│              Currently: EMPTY                               │
│              { }                                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Luka's check_interference arrives
                              │ Symbols: ["auth.ts::validateToken",
                              │           "auth.ts::login"]
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Tool Execution:                                            │
│                                                             │
│  1. Authenticate: user = "luka"                             │
│  2. Check lock_table for each symbol                        │
│     → "auth.ts::validateToken" NOT FOUND ✓                 │
│     → "auth.ts::login" NOT FOUND ✓                         │
│  3. Check dependency graph for warnings                     │
│     → validateToken depends on: ["jwt.ts::verify"]         │
│     → jwt.ts::verify status: OPEN ✓                        │
│     → No dependencies being WRITTEN ✓                      │
│                                                             │
│  Return: {                                                  │
│    conflicts: [],         ← CLEAR!                         │
│    warnings: []           ← No dependency conflicts        │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────┐
│  Agent A (Luka) receives:   │
│                             │
│  "✅ No conflicts detected  │
│   Safe to proceed!"         │
│                             │
│  Agent decides:             │
│  → Start reading code       │
└─────────────┬───────────────┘
              │
              │ post_status()
              │ symbol: "auth.ts::validateToken"
              │ status: "READING"
              │ message: "Analyzing auth flow"
              │
              │ post_activity()
              │ message: "Starting auth refactor - analyzing current implementation"
              │ layer: "backend/auth"
              │
              ▼

TIME: T=2 (Luka posts reading status)
════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│              MCP Server: Lock Table UPDATED                 │
│                                                             │
│  {                                                          │
│    "auth.ts::validateToken": {                             │
│      user_id: "luka",                                      │
│      user_email: "luka@example.com",                       │
│      status: "READING",  🔵                                │
│      message: "Analyzing auth flow",                       │
│      timestamp: 1000,                                      │
│      last_heartbeat: 1000                                  │
│    }                                                        │
│  }                                                          │
│                                                             │
│  activity_feed:                                            │
│  [{                                                         │
│    user_name: "Luka",                                      │
│    message: "Starting auth refactor - analyzing current    │
│              implementation",                              │
│    layer: "backend/auth"                                   │
│  }]                                                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket broadcast
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│            Graph Visualization UI Updates                   │
│                                                             │
│  Before: ⚪ validateToken (idle/OPEN)                      │
│  After:  🔵 validateToken (Luka - READING)                 │
│                                                             │
│  Folder View:                                              │
│  📁 src/auth/  🔵 Luka is reading here                     │
│                                                             │
│  Agent Activity Feed:                                      │
│  [12:00:02] Luka: Starting auth refactor - analyzing...    │
└─────────────────────────────────────────────────────────────┘

TIME: T=3 (Luka transitions to WRITING)
═════════════════════════════════════════

┌─────────────────────────────┐
│  Agent A (Luka)             │
│                             │
│  Finished reading.          │
│  Now writing code.          │
│                             │
│  Calls: post_status()       │
│  status: "WRITING"          │
│                             │
│  Calls: post_activity()     │
│  message: "Implementing JWT"│
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│              MCP Server: Lock Table UPDATED                 │
│                                                             │
│  {                                                          │
│    "auth.ts::validateToken": {                             │
│      user_id: "luka",                                      │
│      user_email: "luka@example.com",                       │
│      status: "WRITING",  🔴 ← Changed!                     │
│      message: "Implementing JWT validation",               │
│      timestamp: 1000,                                      │
│      last_heartbeat: 1030                                  │
│    }                                                        │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket broadcast
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│            Graph Visualization UI Updates                   │
│                                                             │
│  Before: 🔵 validateToken (Luka - READING)                 │
│  After:  🔴 validateToken (Luka - WRITING)                 │
│                                                             │
│  Folder View:                                              │
│  📁 src/auth/  🔴 Luka is WRITING here                     │
│                                                             │
│  Agent Activity Feed:                                      │
│  [12:00:30] Luka: Implementing JWT validation              │
│  [12:00:02] Luka: Starting auth refactor - analyzing...    │
└─────────────────────────────────────────────────────────────┘

TIME: T=4 (Jane's check arrives - CONFLICT!)
═══════════════════════════════════════════════

                             ┌─────────────────────────────┐
                             │  Agent B (Jane)             │
                             │                             │
                             │  Calls: check_interference()│
                             │  Symbols:                   │
                             │  ["auth.ts::validateToken", │
                             │   "auth.ts::resetPassword"] │
                             └─────────────┬───────────────┘
                                           │
                                           ▼
┌─────────────────────────────────────────────────────────────┐
│  MCP Server: Conflict Detection                             │
│                                                             │
│  1. Authenticate: user = "jane"                             │
│                                                             │
│  2. Check each symbol:                                      │
│                                                             │
│     Symbol: "auth.ts::validateToken"                       │
│     ├─ Check lock_table                                    │
│     ├─ FOUND! {                                            │
│     │    user_id: "luka",                                  │
│     │    status: "WRITING"  ← 🔴 HARD CONFLICT!           │
│     │  }                                                    │
│     ├─ Is it Jane? NO (it's Luka)                          │
│     └─ Add to conflicts with severity: HIGH                │
│                                                             │
│     Symbol: "auth.ts::resetPassword"                       │
│     ├─ Check lock_table → NOT FOUND ✓                     │
│     ├─ Check dependencies: ["auth.ts::validateToken"]      │
│     ├─ validateToken is WRITING by Luka!                   │
│     └─ Add to warnings: DEPENDENCY_CONFLICT                │
│                                                             │
│  Return: {                                                  │
│    your_email: "jane@example.com",                         │
│    conflicts: [                                            │
│      {                                                     │
│        symbol: "auth.ts::validateToken",                   │
│        held_by_email: "luka@example.com",                  │
│        held_by_name: "Luka",                               │
│        status: "WRITING",                                  │
│        message: "Implementing JWT validation",             │
│        severity: "HIGH"  ← ⚠️ DANGER!                      │
│      }                                                     │
│    ],                                                       │
│    warnings: [                                             │
│      {                                                     │
│        type: "DEPENDENCY_CONFLICT",                        │
│        dependency: "auth.ts::validateToken",               │
│        held_by_name: "Luka",                               │
│        message: "Dependency is being WRITTEN, may have     │
│                 problems"                                  │
│      }                                                     │
│    ]                                                       │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Agent B (Jane) receives conflict info                      │
│                                                             │
│  System prompt rule:                                       │
│  "If severity = HIGH, MUST ask user for guidance"          │
│                                                             │
│  Agent generates message:                                  │
└─────────────────────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│              Jane's Cursor UI Shows:                        │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ ⚠️ Conflict Detected                                  │ │
│  │                                                       │ │
│  │ DIRECT CONFLICT:                                      │ │
│  │ • auth.ts::validateToken is being WRITTEN by Luka    │ │
│  │   "Implementing JWT validation"                       │ │
│  │                                                       │ │
│  │ DEPENDENCY WARNING:                                   │ │
│  │ • auth.ts::resetPassword depends on validateToken    │ │
│  │ • validateToken is being modified, you may           │ │
│  │   experience problems if you proceed                 │ │
│  │                                                       │ │
│  │ Options:                                              │ │
│  │ 1. ⏸️ Wait for Luka to finish                         │ │
│  │ 2. ⚡ Proceed anyway (risky - may cause conflicts)    │ │
│  │ 3. 🎯 Work on something else for now                  │ │
│  │                                                       │ │
│  │ What would you like me to do?                         │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
              │
              │ Jane (the human) chooses option 3
              │
              ▼
┌─────────────────────────────┐
│  Agent B (Jane)             │
│                             │
│  Revised plan:              │
│  "I'll investigate the DB   │
│   timeout issue instead"    │
│                             │
│  Calls: check_interference()│
│  Symbols:                   │
│  ["db.ts::query"]           │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│  MCP Server: Second Check                                   │
│                                                             │
│  Symbol: "db.ts::query"                                    │
│  ├─ Check lock_table → NOT FOUND ✓                        │
│  ├─ Check dependencies → NOT FOUND ✓                      │
│  └─ CLEAR!                                                 │
│                                                             │
│  Return: {                                                  │
│    conflicts: [],  ← CLEAR!                                │
│    warnings: []    ← No dependency issues                  │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Agent B (Jane)             │
│                             │
│  "✅ Safe to proceed!"      │
│                             │
│  Calls: post_status()       │
│  symbol: "db.ts::query"     │
│  status: "READING"          │
│                             │
│  Calls: post_activity()     │
│  message: "Investigating DB │
│           timeout issue"    │
│  layer: "backend/db"        │
└─────────────┬───────────────┘
              │
              ▼

TIME: T=5 (Both agents working - NO OVERLAP!)
════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│              MCP Server: Lock Table - Parallel Work         │
│                                                             │
│  lock_table: {                                             │
│    "auth.ts::validateToken": {                             │
│      user_id: "luka",                                      │
│      user_email: "luka@example.com",                       │
│      status: "WRITING",  🔴                                │
│      message: "Implementing JWT validation"                │
│    },                                                       │
│    "db.ts::query": {                                       │
│      user_id: "jane",                                      │
│      user_email: "jane@example.com",                       │
│      status: "READING",  🔵                                │
│      message: "Investigating timeout issue"                │
│    }                                                        │
│  }                                                          │
│                                                             │
│  activity_feed: [                                          │
│    {user: "Jane", message: "Investigating DB timeout",     │
│     layer: "backend/db"},                                  │
│    {user: "Luka", message: "Implementing JWT validation",  │
│     layer: "backend/auth"}                                 │
│  ]                                                          │
└─────────────────────────────────────────────────────────────┘
              │
              │ WebSocket broadcasts to all clients
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│            Graph Visualization - BOTH AGENTS VISIBLE        │
│                                                             │
│  📁 FOLDER VIEW:                                           │
│  ├─ 📁 src/auth/  🔴 Luka is WRITING here                  │
│  └─ 📁 src/db/    🔵 Jane is READING here                  │
│                                                             │
│  📄 SYMBOL VIEW:                                           │
│    ┌───────────┐              ┌───────────┐               │
│    │  auth.ts  │              │   db.ts   │               │
│    └─────┬─────┘              └─────┬─────┘               │
│          │                           │                      │
│     ┌────┴────┬───────────           ▼                     │
│     ▼         ▼                  ┌────────┐                │
│  ┌────────┐ ┌────────┐           │ query  │                │
│  │validate│ │ login  │           │  🔵    │                │
│  │Token   │ │        │           │  Jane  │                │
│  │  🔴    │ │ (OPEN) │           │READING │                │
│  │  Luka  │ │        │           └────────┘                │
│  │WRITING │ │        │                                      │
│  └────────┘ └────────┘                                     │
│                                                             │
│  DIFFERENT FOLDERS = ✅ SAFE COLLABORATION                 │
│                                                             │
│  Agent Activity Feed (Slack):                              │
│  [12:00:50] Jane: Investigating DB timeout issue           │
│             Layer: backend/db                              │
│  [12:00:30] Luka: Implementing JWT validation              │
│             Layer: backend/auth                            │
│  [12:00:02] Luka: Starting auth refactor - analyzing...    │
│             Layer: backend/auth                            │
└─────────────────────────────────────────────────────────────┘

══════════════════════════════════════════════════════════════════

KEY INSIGHTS FROM THIS FLOW:

✅ Conflict Detected BEFORE Code Changes
   → Agents check first, write second

✅ Dependency Warnings
   → If dependency is being WRITTEN, agent is warned
   → "validateToken is being modified, you may have problems"
   → Prevents cascading conflicts

✅ Human-Readable Context
   → "Your teammate Luka" not "agent_a"
   → Shows actual status and message

✅ Three-State Model
   → OPEN: Available (holds git timestamp)
   → READING: Analyzing code
   → WRITING: Actively modifying

✅ Git Integration
   → OPEN status tracks last commit info
   → Agents handle git operations (commit, push, pull)
   → MCP server only tracks coordination state

✅ User Stays in Control
   → Agent presents options with full context
   → Human chooses strategy
   → Agent adapts accordingly

✅ Real-Time Visibility
   → Graph shows symbol-level status
   → Folder view shows which directories are active
   → Agent activity feed (like Slack) for high-level updates
   → WebSocket keeps everything synced

✅ Parallel Work When Safe
   → Different folders/symbols = no conflict
   → Both agents progress independently
   → Git merge happens smoothly

══════════════════════════════════════════════════════════════════
```

---

## Diagram 4: Complete Git Integration & Staleness Protocol

```
┌─────────────────────────────────────────────────────────────────────────────┐
│           GIT FRESHNESS MODEL: REPO-LEVEL HEAD TRACKING                    │
└─────────────────────────────────────────────────────────────────────────────┘

KEY PRINCIPLE: Freshness is tracked at REPO HEAD, not per-file
═══════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────┐
│  MCP Server State                                            │
│                                                              │
│  repo_head: "abc123def"  ← Latest known HEAD                │
│                                                              │
│  lock_table: {                                              │
│    "auth.ts::validateToken": {                             │
│      status: "WRITING",                                    │
│      user_id: "luka",                                      │
│      agent_head: "abc123def"  ← Agent's HEAD when locked  │
│    }                                                        │
│  }                                                          │
└──────────────────────────────────────────────────────────────┘

AGENT WORKFLOW: ENTERING WRITING STATE
════════════════════════════════════════════════════════════════

┌─────────────────────────────┐
│  Step 1: Check Status       │
│                             │
│  Agent calls:               │
│  check_status()             │
│    ↓                        │
│  Returns: {                 │
│    repo_head: "abc123",     │
│    locks: {...},            │
│    recent_activity: [...]   │
│  }                          │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 2: Compare HEADs      │
│                             │
│  agent_head = git rev-parse │
│               HEAD          │
│  # "abc123"                 │
│                             │
│  if agent_head != repo_head:│
│    → STALE! Must pull       │
│  else:                      │
│    → Fresh, can proceed     │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 3a: If STALE          │
│                             │
│  git pull --rebase          │
│    ↓                        │
│  New agent_head: "def456"   │
│    ↓                        │
│  Re-run check_status()      │
│    ↓                        │
│  Verify no new conflicts    │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 3b: If Fresh          │
│                             │
│  Proceed to post_status()   │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 4: Claim Lock         │
│                             │
│  post_status(               │
│    symbol="auth.ts::...",   │
│    status="WRITING",        │
│    agent_head="abc123"      │
│  )                          │
│    ↓                        │
│  MCP Server checks:         │
│  - Is symbol locked?        │
│  - Is agent_head == repo?   │
│    ↓                        │
│  If valid → Lock granted    │
│  If stale → REJECTED        │
└─────────────────────────────┘

REJECTION RESPONSE (STALE REPO)
════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────┐
│  MCP Response when agent is stale:                           │
│                                                              │
│  {                                                           │
│    "status": "REJECTED",                                    │
│    "reason": "STALE_REPO",                                  │
│    "message": "Your local repo is behind the latest         │
│                changes. Pull and retry.",                   │
│    "server_repo_head": "abc123def",                         │
│    "your_repo_head": "old789xyz",                           │
│    "required_action": "git pull --rebase"                   │
│  }                                                           │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  Agent MUST:                                                 │
│                                                              │
│  1. git pull --rebase                                       │
│  2. Re-run check_status() to get latest state               │
│  3. Verify no new conflicts appeared                        │
│  4. Retry post_status(WRITING) with new agent_head          │
│                                                              │
│  This is HARD FAIL + RETRY semantics.                       │
└──────────────────────────────────────────────────────────────┘

AGENT WORKFLOW: RELEASING CONTROL (GOING TO OPEN)
════════════════════════════════════════════════════════════════

┌─────────────────────────────┐
│  Step 1: Complete Work      │
│                             │
│  Agent finishes editing     │
│  files locally              │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 2: Commit Changes     │
│                             │
│  git add .                  │
│  git commit -m "..."        │
│    ↓                        │
│  New HEAD: "xyz789abc"      │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 3: Pull & Push        │
│                             │
│  git pull --rebase          │
│    ↓                        │
│  Resolve conflicts if any   │
│    ↓                        │
│  git push                   │
│    ↓                        │
│  Final HEAD: "final123"     │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  Step 4: Release Lock       │
│                             │
│  new_repo_head = git        │
│    rev-parse HEAD           │
│  # "final123"               │
│                             │
│  post_status(               │
│    symbol="auth.ts::...",   │
│    status="OPEN",           │
│    new_repo_head="final123" │
│  )                          │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│  MCP Server Validation:                                     │
│                                                             │
│  1. Did repo_head advance?                                 │
│     old: "abc123def"                                       │
│     new: "final123"                                        │
│     ✓ Advanced                                             │
│                                                             │
│  2. Update server state:                                   │
│     repo_head = "final123"                                 │
│     lock_table["auth.ts::validateToken"] = {              │
│       status: "OPEN",                                      │
│       last_commit_sha: "final123",                         │
│       last_commit_author: "luka",                          │
│       last_commit_time: <query git>                        │
│     }                                                       │
│                                                             │
│  3. Broadcast: "lock_released" event                       │
└─────────────────────────────────────────────────────────────┘

REJECTION: HEAD DID NOT ADVANCE
════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────┐
│  If agent tries to release with unchanged HEAD:              │
│                                                              │
│  {                                                           │
│    "status": "REJECTED",                                    │
│    "reason": "HEAD_NOT_ADVANCED",                           │
│    "message": "You must commit and push before releasing.   │
│                Expected new HEAD, got same as before.",     │
│    "server_repo_head": "abc123",                            │
│    "your_repo_head": "abc123",  ← Same!                     │
│    "required_action": "git commit && git push"              │
│  }                                                           │
└──────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

MCP RESPONSIBILITIES (WHAT MCP DOES):
═══════════════════════════════════════════════════════════════

✅ Track repo_head (latest known HEAD)
✅ Validate agent_head matches repo_head before granting WRITING lock
✅ Reject stale operations with clear error messages
✅ Update repo_head when agent releases with new_repo_head
✅ Query git for commit metadata (when status goes OPEN)
✅ Enforce intent transitions (OPEN ↔ READING ↔ WRITING)
✅ Broadcast events to connected clients

MCP DOES NOT DO:
═══════════════════════════════════════════════════════════════

❌ Run any git commands itself
❌ Commit files
❌ Push to remote
❌ Merge branches
❌ Resolve conflicts
❌ Edit filesystem directly
❌ Enforce per-file locks (uses symbols/functions)
❌ Track per-file dependency graph (optional future feature)
❌ Provide chat/threading

AGENT RESPONSIBILITIES (WHAT AGENTS DO):
═══════════════════════════════════════════════════════════════

✅ Run all git operations (pull, commit, push)
✅ Check status before entering WRITING
✅ Compare agent_head vs repo_head
✅ Pull + rebase if stale
✅ Edit files locally (Read/Write/Edit tools)
✅ Commit changes with descriptive messages
✅ Push to remote before releasing control
✅ Post status updates to MCP
✅ Handle merge conflicts if they occur
✅ Retry failed operations after fixing staleness

═══════════════════════════════════════════════════════════════════════════════
```

---

## Protocol Design: Tool Usage Contracts

### **MCP Tool Descriptions Embed the Protocol**

Each MCP tool description includes the exact sequence agents must follow. This becomes the primary "how to use the server" instruction surface (vs a giant system prompt).

---

### **Tool 1: check_status**

**Description:**
```
Get current coordination state before taking action.

RETURNS:
  - repo_head: Latest known repository HEAD
  - locks: Who is READING/WRITING what symbols
  - recent_activity: Last N agent activity messages
  - stale_status: Whether your repo is behind

USAGE CONTRACT:
  1. Call this BEFORE entering WRITING state
  2. Compare your local HEAD: git rev-parse HEAD
  3. If your_head != repo_head:
     → You are STALE
     → git pull --rebase
     → Re-run check_status
     → Verify no new conflicts
  4. If fresh, proceed to post_status(WRITING)

RESPONSE SCHEMA:
{
  "repo_head": "abc123def",
  "locks": {
    "auth.ts::validateToken": {
      "status": "WRITING",
      "user_name": "Luka",
      "agent_head": "abc123def"
    }
  },
  "recent_activity": [
    {
      "timestamp": 1234567890,
      "user": "Luka",
      "scope": ["auth.ts"],
      "intent": "WRITING",
      "summary": "Refactoring validateToken to JWT"
    }
  ],
  "stale_status": {
    "is_stale": false,
    "your_head": "abc123def",
    "server_head": "abc123def"
  }
}
```

---

### **Tool 2: post_status**

**Description:**
```
Update your intent for a symbol: OPEN, READING, or WRITING.

PARAMETERS:
  - symbol: Code symbol (e.g., "auth.ts::validateToken")
  - status: "OPEN" | "READING" | "WRITING"
  - message: Short description of what you're doing
  - agent_head: (required for WRITING) Your current repo HEAD
  - new_repo_head: (required for OPEN) New HEAD after your commit

USAGE CONTRACT:

FOR ENTERING WRITING:
  1. Call check_status first
  2. Ensure your repo is fresh (pull if needed)
  3. Call post_status(symbol, "WRITING", agent_head=<your HEAD>)
  4. If REJECTED with STALE_REPO:
     → git pull --rebase
     → Re-run check_status
     → Retry post_status

FOR RELEASING CONTROL (OPEN):
  1. Complete your edits
  2. git commit -m "descriptive message"
  3. git pull --rebase
  4. Resolve any conflicts
  5. git push
  6. Get new HEAD: git rev-parse HEAD
  7. Call post_status(symbol, "OPEN", new_repo_head=<new HEAD>)
  8. If REJECTED with HEAD_NOT_ADVANCED:
     → You forgot to commit or push
     → Fix and retry

REJECTION RESPONSES:
  - STALE_REPO: Your local repo is behind, pull and retry
  - SYMBOL_LOCKED: Someone else is WRITING this symbol
  - HEAD_NOT_ADVANCED: (for OPEN) Must commit + push first

SUCCESS RESPONSE:
{
  "status": "SUCCESS",
  "symbol": "auth.ts::validateToken",
  "new_status": "WRITING",
  "repo_head": "abc123def"
}

ERROR RESPONSE:
{
  "status": "REJECTED",
  "reason": "STALE_REPO",
  "message": "Your local repo is behind. Pull and retry.",
  "server_repo_head": "abc123def",
  "your_repo_head": "old789xyz",
  "required_action": "git pull --rebase"
}
```

---

### **Tool 3: post_activity**

**Description:**
```
Post a high-level activity update to the shared feed (like Slack).

USE THIS FOR:
  - Semantic boundaries (starting a new phase of work)
  - Context switches (moving to different layer/module)
  - Significant milestones (completed refactor, running tests)

DO NOT USE FOR:
  - Every file edit (too noisy)
  - Chat messages (not a chat system)
  - Debugging (use your own logs)

PARAMETERS:
  - summary: Brief description (1-2 sentences max)
  - scope: List of files/symbols you're working on
  - intent: "READING" | "WRITING" | "TESTING" | "DEBUGGING"

SCHEMA:
{
  "timestamp": <auto-generated>,
  "user": <from auth>,
  "scope": ["auth.ts", "jwt.ts"],
  "intent": "WRITING",
  "summary": "Refactoring validateToken to JWT-based auth"
}

WHEN TO POST:
  ✓ Starting WRITING on a new area
  ✓ Switching from one module to another
  ✓ Finishing a major task
  ✓ Encountering unexpected blockers

WHEN NOT TO POST:
  ✗ Every status change (use post_status for that)
  ✗ Conversational messages
  ✗ Progress percentages
```

---

### **Tool 4: heartbeat**

**Description:**
```
Keep your locks alive (60s timeout).

USAGE:
  - Call every 30-45 seconds while WRITING
  - Prevents lock expiration
  - No-op if you don't hold any locks

PARAMETERS:
  - symbols: List of symbols you're keeping alive

AUTO-EXPIRATION:
  If you don't heartbeat for >60s:
  - Lock is released automatically
  - Status set to OPEN
  - Event broadcast: "lock_expired"
```

---

## Intentional Agent Messages: Activity Feed Schema

**Activity Feed is NOT chat. It's short, high-signal updates.**

### **Message Schema:**
```json
{
  "timestamp": "2024-01-15T12:34:56Z",
  "user": "Luka",
  "scope": ["auth.ts", "jwt.ts"],
  "intent": "WRITING",
  "summary": "Refactoring validateToken to JWT-based auth"
}
```

### **When to Post:**
- **Semantic boundaries**: Starting WRITING, changing direction, finishing
- **Context switches**: Moving to different layer/module
- **Significant events**: Completed refactor, running tests, found blocker

### **When NOT to Post:**
- Every file edit (too noisy)
- Status changes (use `post_status` for that)
- Chat/conversation (not a messaging system)
- Internal debugging info

### **UI Display:**
Simple "Recent Agent Activity" panel showing last N messages:
```
Recent Agent Activity:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[12:34:56] Luka - WRITING
           Scope: auth.ts, jwt.ts
           Refactoring validateToken to JWT-based auth

[12:30:15] Jane - DEBUGGING
           Scope: db.ts
           Investigating connection timeout in query pool

[12:28:42] Luka - READING
           Scope: auth.ts
           Analyzing current authentication flow
```

---

## UI Responsibilities (Clarified)

### **UI Displays:**
- Current locks/statuses (symbol-level and folder-level)
- Current `repo_head`
- Activity feed messages
- "Client is stale" indicator (if `your_head != repo_head`)
- Real-time updates via WebSocket

### **UI Does NOT:**
- Run git commands
- Enforce any rules
- Commit or push code
- Resolve conflicts
- Control file access

**UI is purely observational + real-time display.**

---

## Dedalus Auth Role (Clarified)

**Used For:**
- ✅ Stable per-agent identity (who locked/posted)
- ✅ Reliable attribution across sessions
- ✅ Multi-tenant isolation
- ✅ Making MCP the single trusted coordination surface
- ✅ Preventing spoofing/impersonation

**How It Works:**
1. Each agent has unique API key: `coord_sk_luka_abc123`
2. Encrypted client-side, decrypted just-in-time by Dedalus
3. MCP server uses key to authenticate: `user = authenticate(api_key)`
4. All operations tagged with `user_id`, `user_email`, `user_name`

---

## Explicitly NOT Doing (Design Constraints)

To keep the hackathon scope manageable:

❌ **Server-side git operations** - Agents handle all git
❌ **Merge orchestration** - Agents resolve conflicts locally
❌ **Chat threads/replies** - Activity feed is one-way broadcast
❌ **Filesystem-level enforcement** - No file permissions/locks
❌ **Per-file dependency graph as source of truth** - Optional future feature
❌ **Automatic conflict resolution** - Human decides
❌ **Code review workflows** - Out of scope
❌ **Branch management** - Agents work on shared branch

---

## Architecture Design Decisions Summary

### 1. **Status Model: OPEN → READING → WRITING**

**Three States:**
- `OPEN`: Symbol is available, holds last git commit metadata
- `READING`: Agent is analyzing/reading the code
- `WRITING`: Agent is actively modifying the code

**Flow:**
```
OPEN → READING → WRITING → OPEN (via git commit)
```

**OPEN Status Special Behavior:**
When an agent posts `status: "OPEN"`, the MCP server:
1. Queries git for the last commit affecting that file
2. Stores commit metadata: SHA, timestamp, author, message
3. Other agents can see when the code was last changed

### 2. **Git Integration Strategy**

**Division of Responsibilities:**
- **MCP Server**: Tracks coordination state (who's working on what)
- **Agents**: Handle all git operations (commit, push, pull)

**Agent Workflow:**
1. Check interference via MCP
2. Post status "READING" via MCP
3. Read files locally
4. Post status "WRITING" via MCP
5. Edit files locally
6. Commit + push via git (agent does this)
7. Post status "OPEN" via MCP (server queries git)

**Why This Works:**
- Agents use normal file operations (Read/Write/Edit tools)
- Git operations are standard (no special server-side logic)
- MCP server stays lightweight (just coordination)
- Each agent works in their own git worktree or clone

### 3. **Dependency Warnings**

**Enhanced Conflict Detection:**
When `check_interference()` is called, the server checks:
1. **Direct conflicts**: Is the symbol being READING or WRITING?
2. **Dependency conflicts**: Are any dependencies being WRITTEN?

**Example:**
```
Jane wants to edit: auth.ts::resetPassword
Dependencies: [auth.ts::validateToken]

If validateToken is being WRITTEN by Luka:
→ WARNING: "Dependency validateToken is being modified,
           you may experience problems"
```

**Agent Response:**
- Shows both direct conflicts AND dependency warnings
- Lets human decide whether to proceed or wait
- Prevents cascading conflicts

### 4. **Agent Activity Feed ("Agent Slack")**

**Purpose:** High-level narrative updates about what agents are doing

**Two Levels of Updates:**
1. **Status updates** (fine-grained): "READING auth.ts::validateToken"
2. **Activity feed** (coarse-grained): "Moving to database layer to investigate timeout"

**New MCP Tools:**
```
post_activity(message, layer, task_type)
  → "Starting authentication refactor"
  → Layer: "backend/auth"
  → Type: "refactor"

get_activity_feed(since_timestamp)
  → Returns recent activity from all agents
```

**UI Display:**
```
Agent Activity Feed:
[12:03:45] Luka: Auth refactor complete, running tests
           Layer: backend/auth
[12:01:30] Jane: Investigating DB connection timeout
           Layer: backend/db
```

### 5. **Folder-Level Visualization**

**Aggregated View:**
The UI shows which folders agents are working in:

```
📁 src/auth/  🔴 Luka is WRITING here
📁 src/db/    🔵 Jane is READING here
📁 src/api/   ⚪ No activity
```

**Computed from lock_table:**
```python
# Server computes folder activity on-the-fly
folder_activity = {
  "src/auth/": {
    users: ["Luka"],
    symbols: ["validateToken", "login"],
    statuses: ["WRITING"]
  }
}
```

**Benefits:**
- Quick overview of where work is happening
- Easier to identify safe areas to work
- Visual separation of concerns

### 6. **Complete MCP Tools List**

**Coordination Tools:**
- `check_interference(symbols)` → Returns conflicts + dependency warnings
- `post_status(symbol, status, message)` → OPEN/READING/WRITING
- `heartbeat(symbols)` → Keep locks alive (60s timeout)

**Activity Feed Tools:**
- `post_activity(message, layer, task_type)` → Post to activity feed
- `get_activity_feed(since_timestamp)` → Get recent updates

**Query Tools:**
- `get_folder_activity()` → Get aggregated folder status

**Removed:**
- ~~`release(symbols)`~~ → Use `post_status(symbol, "OPEN")` instead

### 7. **WebSocket Events**

**Broadcast Events:**
- `status_update` → Symbol status changed (OPEN/READING/WRITING)
- `activity_posted` → New activity feed message
- `lock_expired` → Lock timed out (no heartbeat for 60s)
- `conflict_warning` → Direct conflict detected
- `dependency_warning` → Dependency being WRITTEN

### 8. **Data Structures**

**lock_table:**
```python
{
  "auth.ts::validateToken": {
    status: "WRITING",
    user_id: "luka",
    user_email: "luka@example.com",
    user_name: "Luka",
    message: "Implementing JWT",
    timestamp: 1234567890.0,
    last_heartbeat: 1234567920.0
  },
  "auth.ts::login": {
    status: "OPEN",  # Available
    user_id: null,
    last_commit_sha: "abc123",
    last_commit_time: 1234567800.0,
    last_commit_author: "jane",
    last_commit_message: "feat: improve login"
  }
}
```

**activity_feed:**
```python
[
  {
    id: "act_001",
    user_name: "Luka",
    message: "Moving to database layer",
    timestamp: 1234567890.0,
    context: {
      layer: "backend/db",
      task_type: "refactor"
    }
  }
]
```

**folder_activity (computed):**
```python
{
  "src/auth/": {
    users: ["Luka"],
    symbols: ["validateToken"],
    statuses: ["WRITING"]
  }
}
```

This visual architecture gives you the complete picture without any code! 🎨
